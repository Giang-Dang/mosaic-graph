<Project Sdk="Microsoft.NET.Sdk.Web">

  <!-- No source generator and no descriptor classes. The SDL in Program.cs is
       the schema; C# only supplies the runtime types and the resolvers. -->

  <ItemGroup>
    <PackageReference Include="HotChocolate.AspNetCore" />
    <PackageReference Include="HotChocolate.AspNetCore.CommandLine" />
  </ItemGroup>

</Project>
