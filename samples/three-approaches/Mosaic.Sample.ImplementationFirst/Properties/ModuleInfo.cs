// Assembly-scoped, not folder-scoped. The generator collects every type marked
// with [QueryType] or [ObjectType<T>] in this assembly and emits one extension
// method named after this attribute: Module("Catalog") gives AddCatalog().
[assembly: Module("Catalog")]
