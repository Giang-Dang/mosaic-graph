// One assembly, one module, one registration method: Module("ExecutorInternals")
// is what makes the generated extension method AddExecutorInternals() rather
// than a name derived from the assembly. Same convention as the six services
// and the other samples.
[assembly: Module("ExecutorInternals")]
